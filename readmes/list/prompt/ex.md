






❯ /init Include detailed notes on definig MCP tools from the README file 

Build a new tool called 'document_path_to_markdown'. It should take in the path to a PDF or DOCX file, read the file, then convert its contents to markdown and return the result. Write out a plan to implement this feature. Don't write any code yet.




Think of some tests to write to evaluate a new tool called 'document_path_to_markdown'. It should take in the path to a PDF or DOCX file, read the file, then convert its
contents to markdown and return the result. Don't write any code yet.